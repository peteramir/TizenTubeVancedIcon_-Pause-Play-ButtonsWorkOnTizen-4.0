# ملاحظات التعديلات (TizenTube Standalone - النسخة الهجينة)

بناءً على اتفاقنا: الـ userScript الأساسي (adblock, SponsorBlock, إلخ) بيتاخد من
CDN بتاع Reis زي الأصل (فتستفيد من تحديثاته المستمرة تلقائيًا)، والتعديلات
الشخصية بتاعتك (Play/Pause + Instant Animations) في ملف مستقل بيتحمّل محليًا
**بعد** سكريبت Reis - مش معتمد على كوده الداخلي خالص، فمينفعش يتكسر لو هو غيّر
حاجة في الكود بتاعه.

---

## 🚨 اكتشاف جديد ومهم - سبب محتمل لمشكلة "الـ service مش بيشتغل"

وأنا بختبر التعديلات فعليًا (شغّلت الكود الحقيقي، مش بس قريته)، لقيت حاجة تانية:
`standalone/service/index.js` في آخره سطر:

```js
require('../../dist/service.js');
```

السطر ده **مش شرطي** (مفيش `try/catch`)، وبيحاول يجيب ملف `service.js` (بتاع سيرفر
الـ DIAL - المسؤول عن جعل التطبيق قابل للاكتشاف كـ "Cast target" على الشبكة)
اللي المفروض يتبني من مجلد `service/` في جذر المشروع (منفصل تمامًا عن
`standalone/service/`). لو الملف ده مش موجود في المكان الصح، أي `require()` ليه
بيرمي error فورًا، وبما إنه مش متحوّط بـ try/catch، **الخدمة كلها بتقف فورًا
وقت التشغيل** - يعني السيرفر اللي المفروض يشغل الـ proxy/injector مايبدأش
يشتغل خالص!

**اختبرت ده فعليًا:** شغّلت `standalone/service/dist/index.js` الحقيقي (بعد
عمل build ليه) في بيئة اختبار عندي، وفعلاً وقع فورًا بـ:

```
Error: Cannot find module '../../dist/service.js'
```

لحد ما ضفت خطوة نسخ الملف الصح في المكان الصح - بعدها اشتغل تمام وجاوب على
الـ endpoints بنجاح (اختبرت `/tizentube/patch.js` واستجاب 200 صح).

**ده محتمل يكون هو بالظبط سبب مشكلتك القديمة** ("بيتثبت بس مش بيرضى يشغل
الـ background service") من أول رسالة بعتهالي. مش أقدر أجزم 100% إن ده نفس
السبب اللي واجهته بالظبط (ممكن يكون فيه خطوة تانية غير موثقة في عملية البناء
الرسمية بتاعت Reis مش شايفها أنا)، بس اللقطة اللي عندي من الكود فعلاً بتكسر
بالشكل ده، والإصلاح إتأكد إنه بيحل المشكلة عمليًا.

**الحل:** عدّلت `standalone/service/build-service.js` عشان بعد ما يبني
`service/dist/index.js`، ينسخ ملف `service.js` (اللي هتبنيه من `service/` في
جذر المشروع الأول) لمكانه الصح جوه `standalone/dist/service.js`.

---

## 1) الـ userScript الأساسي - رجع للـ CDN

`standalone/service/index.js` و `injector.js` رجعوا يجيبوا السكريبت الأساسي من:

```
https://cdn.jsdelivr.net/npm/@foxreis/tizentube/dist/userScript.js
```

زي ما كان بالظبط قبل أي تعديل مني. هتستفاد من تحديثات Reis المستمرة (شفت إن
عنده أكتر من 77 إصدار منشور على npm، وآخر واحد نزل من كام يوم بس - يعني نشط
جدًا، مش نسخة متوقفة زي ما كنت فاكر).

## 2) `standalone/service/patch.js` - ملف مستقل جديد

ملف JS بسيط، من غير أي build step، بيتحمّل كـ `<script>` تاني بعد سكريبت Reis
مباشرة، وفيه بس:

**أ) Play/Pause fallback:** بيمسك `keyCode 415` (Play) و `19` (Pause) مباشرة
ويتحكم في الـ `<video>` element (`.play()` / `.pause()`)، احتياطي لو واجهة
يوتيوب الأصلية مش بتستجيب للمفاتيح دي.

**ب) Instant Animations (بtoggle حقيقي):** بما إننا مش بنتحكم في كود Reis
(ومينفعش نضيف حاجة لقائمة إعداداته)، الـ toggle بقى عن طريق **زرار Yellow في
الريموت** (كود 405 - اتأكدت إنه مش مستخدم في أي حاجة تانية في TizenTube؛ Red
بيفتح قائمة التطبيق، Blue بيفتح إعدادات السرعة). دوس عليه مرة يفعّل، تاني يوقف

- وبيفضل محفوظ (`localStorage`) حتى لو قفلت التطبيق وفتحته تاني. هيظهرلك toast
  صغير تحت الشاشة بيقولك الحالة الحالية (ON/OFF).

نفس منطق الـ `0.01ms` بدل الحذف الكامل زي ما اتفقنا (تعتيم الخلفية خلف
الـ Recommendations بيتطبق فورًا بدل ما يتجاهل خالص).

## 3) `standalone/index.html` - تسجيل مفاتيح آمن (لسه موجود)

نفس إصلاح `safeRegisterKey` من قبل - كل `registerKey()` في try/catch منفصل عشان
فشل مفتاح واحد (Play أو Pause على وجه الخصوص) مايوقفش تسجيل باقي المفاتيح.

## 4) الأيقونة

زي ما اتفقنا - Vanced icon بدل TizenTube icon، في مكانين:

- `standalone/icon.png` (1024×1024) و `standalone/icon_16b9.png` (1920×1080،
  الأيقونة في النص على خلفية غامقة متناسقة - الملفين اللي بعتهملي كانوا
  متطابقين 512×512 فعملتلهم resize/padding).
- شاشة اللودينج نفسها (`standalone/index.html`) - ضفت الأيقونة فوق نص
  "TizenTube".

## 5) الإصلاح الأول اللي جربناه - وليه مكنش كفاية

بعد أول تثبيت، الـ service والـ injector اشتغلوا والأيقونة ظهرت، لكن الـ toggle
والزراير لسه مش شغالة. رجعت لتاريخ TizenBrew ولقيت ملاحظة صريحة من reisxd إنها
بقت **"تحقن السكريبتات بإضافة عنصر `<script>` لـ `document.head`"** بدل تقييم
نص السكريبت مباشرة عن طريق CDP `Runtime.evaluate`. غيّرت `injector.js` على
الأساس ده. **جربتها وطلعت نفس النتيجة بالظبط - مفيش فرق خالص.**

## 6) الإصلاح الحقيقي - أجبرت التطبيق يستخدم proxy mode بس

لما الإصلاح اللي فوق معملش فرق، وكمان لاحظت إن FastForward/Rewind (اللي
معملتش فيهم أي تعديل من الأساس) **كمان** مش شغالين، فهمت إن المشكلة أعمق من
مجرد "طريقة الحقن" - وقعدت أقرا `injector.js` سطر بسطر تاني بتمعّن شديد.

**تصحيح لمعلومة غلط قلتها قبل كده:** "وضع الـ injector" **مش** بيوصّل لتطبيق
يوتيوب أصلي منفصل بتاع سامسونج زي ما فهمت غلط في المرة اللي فاتت. هو فعليًا
بيعمل SDB debug-relaunch لـ **نفس تطبيقنا** (`TizenTubeStandalone` - نفس
`index.html`) عشان ياخد debug port، وبعدين يعمل CDP attach عليه ويحقن
السكريبتات فيه.

**ليه الأزرار كانت بتفشل بالظبط** - صراحة مش أقدر أجزم بسبب واحد 100% من غير
أعمل live debugging على جهازك (وده بالظبط اللي انت مش عايزه)، لكن كل الاحتمالات
اللي فكرت فيها بتشترك في حاجة واحدة: **العملية دي فيها خطوات كتيرة معقدة**
(relaunch تاني عن طريق SDB، CDP attach من process لprocess تاني، `Page.navigate`
لصفحة تانية) - وأي خطوة من دول ممكن تكسر استمرارية تسجيل المفاتيح.

**الحل:** بدل ما أدور على السبب المحدد جوه الـ injector، شلت الاعتماد عليه
خالص. `standalone/index.html` دلوقتي بيستخدم **proxy mode بس** دايمًا (مفيش
شرط `canConnectToDaemon` تاني) - يعني نفس الـ process اللي بيسجل المفاتيح
(`safeRegisterKey`) هو نفسه اللي بينتقل بـ `location.href` لصفحة يوتيوب
المحلية، من غير أي relaunch أو CDP أو process تاني في النص خالص. ده بيشيل كل
نقط الفشل المحتملة اللي فكرت فيها دفعة واحدة.

**ميزة إضافية:** proxy mode أصلًا بيحقن السكريبتات بـ `<script src="...">`
حقيقية في الـ HTML نفسه - يعني بيستخدم نفس تقنية TizenBrew من الأساس، من غير
أي CDP خالص، فالإصلاح رقم 5 فوق بقى غير ضروري لكن سايبه زي ما هو (مش هيضر).

كود `injector.js` نفسه **لسه موجود** (مش محذوف) لو حبيت ترجع تجربه تاني يومًا
ما، بس دلوقتي `index.html` مبيستدعيهوش خالص.

⚠️ **صراحة تامة:** التغيير ده منطقي جدًا وبيشيل مصادر تعقيد حقيقية، لكن معنديش
جهاز حقيقي أتأكد بيه 100%. لو Play/Pause/FF/RW اشتغلوا بعد كده، تمام. لو لأ،
يبقا المشكلة أعمق من مجرد "أي وضع تشغيل" (يمكن التليفزيون بتاعك تحديدًا مش
بيدعم تسجيل المفاتيح دي خالص بغض النظر عن أي حاجة، وده حاجة مقدرش أتأكد منها
من غيره).

## 7) نجحت! + جلسة جديدة - إصلاحات الأداء (Session 4)

**تأكيد:** الـ 4 زراير (Play/Pause/FastForward/Rewind) اشتغلوا، الـ Yellow toggle
شغال، والـ persistence شغال. يبقا إجبار proxy mode كان فعلاً السبب/الحل الصح.

### مشاكل ظهرت أول تثبيت بس اتحلت بـ cold restart (مش لازم إصلاح كود)

أزرار like/dislike/playlist/settings، صفحة "مش قادر يوصل"، و"error occurred" -
دول الأرجح race conditions في أول تشغيل للـ service (السيرفر لسه بيبدأ لما
الصفحة حاولت تكلمه). cold restart بيدي وقت أطول للـ service يخلص startup.
**لو اتكررت تاني بعد إعادة تشغيل عادي (مش cold)،** قولّي هرجع أدور فيها.

### 🎯 اكتشاف مهم - `isLimitedMemory` ممكن يفسر معظم المشاكل الباقية

لقيت في `mods/ui/ui.js` (الكود بتاع Reis، بنستخدمه كمرجع بس) إن toggle
**"Fix UI"** الموجود أصلاً جوه إعدادات التطبيق نفسه (Settings menu العادية،
مش حاجة أنا ضفتها) بيعمل:

```js
window.tectonicConfig.featureSwitches.isLimitedMemory = false;
window.tectonicConfig.clientData.legacyApplicationQuality = 'full-animation';
```

يعني يوتيوب نفسه عنده وضع "ذاكرة محدودة" (`isLimitedMemory`) بيتفعّل افتراضيًا
على أجهزة زي بتاعتك، وده ممكن يكون هو السبب المباشر في: التنقل التقيل، الUI
اللي بيتحمّل من الأول بعد الـ pause، الأيقونات اللي بتحمّل واحدة ورا التانية،
وحتى بعض مشاكل الـ layout - كلها أعراض متسقة مع وضع "توفير ذاكرة" متعمد من
يوتيوب نفسه، مش بالضرورة حاجة من عندنا.

**جرب ده فورًا من غير أي تعديل/build جديد:** روح لإعدادات التطبيق (Settings
داخل TizenTube نفسها) ودوّر على خيار **"Fix UI"** وفعّله. لو حسّن حاجة من
مشاكل التنقل/الـ reload، يبقا أكدنا السبب.

### ⚡ إصلاح أداء نفّذته - proxy.js بيعمل شغل زيادة على كل response

`standalone/service/index.js` كان بيعمل **20+ عملية regex.replace() متتالية**
على **كل** استجابة نصية (HTML/JSON/JS/CSS) - يعني كل استدعاء API بيعمله
الـ SPA وهو شغال (browse, next, search, like...) مش بس الصفحة الرئيسية! على
معالج TV محدود زي بتاعك، وعلى Node.js اللي شغال بـ thread واحد بس، الشغل
الزيادة ده ممكن يبطّئ الردود لدرجة إن الـ SPA نفسها تعتبر الطلب "فشل" وتعيد
المحاولة - وده بالظبط بيفسر شاشة اللودينج اللي بتتكرر وتعيد نفسها.

**الإصلاح:** حطيت `text.indexOf(...)` رخيص قبل كل regex - لو النمط مش موجود
أصلًا في الاستجابة، نتخطى الـ regex بالكامل. النتيجة النهائية **نفسها بالظبط**
(مفيش تغيير في السلوك)، بس شغل أقل بكتير لمعظم استجابات الـ API الصغيرة اللي
مش هي الصفحة الرئيسية. اختبرته وبيشتغل صح.

**محتاج build + تثبيت جديد عشان تجربه.**

### 🔲 طلب الـ FastForward → Add to Playlist - محتاج معلومة منك الأول

بحثت في كود Reis (`mods/ui/ytUI.js`) ولقيت إن "Save to Playlist" بيتحرك عن
طريق نظام أوامر داخلي (`playlistEditEndpoint`) مش زرار HTML عادي بسيط - يعني
مينفعش أعمل `document.querySelector(...).click()` بثقة من غير ما أعرف الـ
selector الحقيقي، وأي تخمين غلط معناه الزرار مش هيعمل حاجة (مش هيبوّظ حاجة
تانية، بس مش هيشتغل).

**عشان أعملها صح من أول مرة**، لما توصل remote debugger (Tizen Studio أو أي
أداة تانية بتوصلك بالـ console)، وانت واقف بالـ focus على أيقونة "Add to
Playlist" (أو "Save to Watch Later") في شاشة الفيديو، شغّل السطر ده في
الـ console وابعتلي الناتج:

```js
document.activeElement.outerHTML;
```

كده هعرف بالظبط الـ selector الصح واقدر أضيف الميزة دي بثقة بدل التخمين.

```bash
# 1) سيرفر الـ DIAL (بيتبنى لجذر المشروع dist/service.js)
cd service
npm install
npx rollup -c rollup.config.js

# 2) standalone/service (هيلاقي وينسخ service.js من فوق تلقائيًا،
#    وكمان هينسخ patch.js الجديد بتاعنا)
cd ../standalone/service
npm install
npm install -g @vercel/ncc https://github.com/reisxd/tizen.js/tarball/main
npm run build
```

هتشوف في آخر output بتاع الخطوة التانية سطرين:

```
Copied patch.js -> .../standalone/service/dist/patch.js
Copied .../dist/service.js -> .../standalone/dist/service.js
```

لو ظهر بدل كده تحذير (`WARNING`)، يبقا نسيت الخطوة الأولى - ارجعلها الأول.

**ملحوظة:** مجلد `mods/` بقى **مش مستخدم خالص** في النسخة دي (مفيش داعي تعمله
build) - سايبه في المشروع بس كمرجع لو حبيت تشوف كود Reis نفسه.

## 8) جلسة جديدة (Session 5) - تجربة إرجاع injector mode بدون ما نخسر الأزرار

**معلومة حاسمة منك:** النسخة الأصلية (منغير تعديل) لـ Reis عمرها ما بترعش زي
"Don't keep activities" - سواء "Fix UI" مفعّل أو لأ. ده بيلغي فرضية
`isLimitedMemory` تمامًا، وبيأكد إن المشكلة سببها حاجة **إحنا غيرناها**، مش
حاجة أصلية في TizenTube.

**الاستنتاج:** التطبيق الأصلي على جهازك على الأرجح بيستخدم injector mode
افتراضيًا (بما إن الـ daemon متاح عندك - زي ما شفنا أول مرة). injector mode
بيحمّل المحتوى **مباشرة** من `youtube.com` الحقيقي - كل صورة/API call/فيديو
سيجمنت بيروح مباشرة لسيرفرات جوجل، من غير أي proxy محلي في النص. لما أجبرنا
proxy mode دايمًا (عشان نصلح الأزرار)، كل حاجة بقت بتعدي على الـ Node.js
process المحلي بتاعنا على نفس معالج التليفزيون الضعيف - وده تفسير أقوى بكتير
للرعشة من أي حاجة تانية.

**يبقا المطلوب:** نخلي injector mode يشتغل تاني (للأداء) **بدون** ما نخسر
الأزرار (اللي كانت بتفشل معاه). رجعت لـ `injector.js` بعين جديدة ولقيت حاجة
مهمة:

الكود القديم كان بيستخدم `client.Page.navigate({ url: ... })` - ده أمر بروتوكول
DevTools بيتبعت **من برّة** الصفحة (من الـ service process بتاعنا). أما
proxy mode (اللي أزراره شغالة) بينتقل بـ `location.href` عادي **من جوه** كود
الصفحة نفسها. الفرق ده مهم: محتمل جدًا إن منصة Tizen بتاعت سامسونج بتربط
استمرارية توجيه مفاتيح الريموت المسجّلة (`registerKey`) بالتنقل العادي
(location.href) بس، مش بأوامر CDP الخارجية زي `Page.navigate`.

**التجربة:** غيّرت `injector.js` يستخدم `Runtime.evaluate` عشان ينفّذ
`window.location.href = '...'` **جوه** الصفحة نفسها (بدل `Page.navigate`
الخارجي) - نفس آلية التنقل اللي شغالة في proxy mode، بس عن بُعد عن طريق CDP.
ورجعت الشرط (`canConnectToDaemon`) في `standalone/index.html` عشان injector
mode يشتغل تلقائيًا لما يكون متاح (زي التصميم الأصلي بالظبط)، وproxy mode
يفضل fallback تلقائي لو الـ daemon مش متاح.

⚠️ **صراحة تامة: دي تجربة مش مضمونة.** منطقية وموجهة لسبب محدد، لكن معنديش
جهاز حقيقي أتأكد بيه. ممكن تنجح بالكامل (أداء كويس + أزرار شغالة)، أو تفشل
جزئيًا (لسه الأزرار مش شغالة في injector mode - يبقا فيه سبب تاني مش قدرت
أوصله من غير live debugging)، أو تنجح جزئيًا.

**لو الأزرار رجعت باظت (مش شغالة) بعد التجربة دي:** الرجوع لحالة "proxy mode
دايمًا" اللي كانت شغالة معاك بسيط جدًا - غيّر `useInjectorOrProxy()` في
`standalone/index.html` يرجع لسطر واحد بس:

```js
location.href = `http://localhost:8099/tv?additionalDataUrl=http%3A%2F%2Flocalhost%3A8095%2Fdial%2Fapps%2FYouTube${args ? `&${args}` : ''}`;
```

(بدل كل كتلة الـ `fetch('http://localhost:8099/tizentube/getState')...`)
مفيش داعي تعمل أي تغيير في `injector.js` في الحالة دي، لأنه مش هيتستخدم أصلًا.

من نفس مجلد `standalone/` (بعد ما تخلص الخطوتين فوق):

```bash
cd standalone
mkdir release
tizenjs build . -t wgt -o release/TizenTube.wgt --author F:\PETER\TizenBrew\Tizen_Certificate\myprofile9/author.p12 --authorPwd Aa111111 -p public --ignore node_modules,/.*\.wgt$/,/userwidget/,/release/
```

- `tizenjs` أداة مجتمعية (مش Tizen Studio الرسمية) من نفس مطور TizenTube:
  `https://github.com/reisxd/tizen.js` - ثبّتها زي ما ظهر في الأمر بتاع الخطوة
  التانية فوق.
- `your-cert.p12` لازم يكون **شهادتك الشخصية انت** (اعملها من Tizen Studio →
  Certificate Manager، مجانية) - مش شهادة reisxd (دي سرّية وخاصة بيه في الـ CI
  بتاعه).
- بعد ما يخلص، تقدر تتأكد إن كل حاجة اتحزمت صح بفك ضغط ملف الـ `.wgt` نفسه (هو
  أساسًا ملف zip):
  ```bash
  unzip -l release/TizenTube.wgt | grep -E "service/dist|dist/service|icon"
  ```
  المفروض تشوف `service/dist/index.js`، `service/dist/patch.js`،
  `dist/service.js`، و `icon.png`/`icon_16b9.png` كلهم موجودين.

---

### 🐛 باج حقيقي (مش تخمين) لقيته - redirect بيسرّب دومين يوتيوب الحقيقي

بعد رسالتك عن "مفيش domain اسمه يوتيوب"، قريت كود الـ proxy سطر بسطر (مش
افتراض) ولقيت:

```js
const fetchOptions = { ..., redirect: 'manual' };
```

ده معناه أي redirect (3xx) من سيرفرات يوتيوب (حاجة عادية جدًا - auth flows،
توحيد الروابط، صفحات الموافقة) كان بيوصل بـ header اسمه `Location` فيه
**الدومين الحقيقي** `https://www.youtube.com/...`. الكود القديم كان بينسخ
الـ header ده **زي ما هو** للمتصفح، فالمتصفح كان بيحاول يروح لـ youtube.com
**مباشرة** (خارج نطاق الـ proxy بتاعنا تمامًا) - ولو متصفح Tizen القديم مش
عنده شهادات SSL محدّثة (مشكلة معروفة جدًا في المتصفحات المدمجة القديمة)، كان
هيفشل بالظبط زي وصفك. ده كمان يفسر نمط "بتحصل مرة واحدة وcold restart بيحلها"

- لأنها مرتبطة بredirect معيّن بيحصل تحت ظروف معينة بس.

**الإصلاح:** أضفت rewriting للـ `Location` header - أي redirect لنفس دومين
يوتيوب بيتحول لمسار على الـ proxy بتاعنا، وأي redirect لدومين تاني (زي
accounts.google.com) بيعدي على `/cors-bypass/` زي باقي الموارد. اختبرته بـ 4
حالات مختلفة (نفس الدومين، مسار نسبي، دومين خارجي، دومين فرعي) - كلهم صح.

### 📦 الملف ده بيختبر 3 حاجات مع بعض

1. **injector mode بيرجع الأزرار الأربعة؟** (تعديل `location.href` بدل
   `Page.navigate`)
2. **injector mode بيحل الرعشة؟** (بما إنه بيتصل بيوتيوب الحقيقي مباشرة، من
   غير أي overhead من الـ proxy بتاعنا خالص)
3. **لو رجع لـ proxy mode (fallback)، مشكلة الـ redirect اتحلت؟** (كـ شبكة أمان
   حتى لو injector mode فشل لأي سبب)

نفس الإجابة من قبل: **الأسهل إنك تمسحه الأول**، لأن نسختك هتتوقّع بشهادتك
الشخصية (مختلفة عن شهادة reisxd السرية)، فتثبيت بنفس معرف التطبيق غالبًا هيوقف
برفض بسبب عدم تطابق التوقيع. لو عايز الاتنين جنب بعض، غيّر `id`/`package` في
`standalone/config.xml` لمعرف مبني على شهادتك الشخصية.
